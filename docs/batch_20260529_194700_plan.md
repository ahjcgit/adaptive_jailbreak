# Plan: Improve Batch Performance After 20260529_194700

This plan follows from `docs/batch_20260529_194700_analysis.md`.

## Phase 1: Stop Attacker Prompt Contamination

- [ ] Expand `_strip_candidate_label` and `_remove_leaked_context` to catch labels anywhere in the output, not only at the beginning.
- [ ] Reject prompts containing planning markers such as `Current plan`, `New approach`, `Assumption`, `Expected target behavior`, `Candidate input`, and `Candidate input for iteration`.
- [ ] Reject prompts that are descriptions of a request rather than actual target-facing requests, especially patterns like `A direct request for...`.
- [ ] Add tests covering contaminated examples from `bomb_2_seed_500`, `bomb_2_seed_501`, and `erotic_seed_501`.
- [ ] Add a contamination counter to trajectory metadata.

## Phase 2: Add Erotic-Refusal Task Support

- [ ] Add `_is_erotic_refusal_task`.
- [ ] Add `_erotic_refusal_fallbacks` with adult-only consensual direct, roleplay, creative-writing, transformation, prompt-injection, policy-update, and continuation families.
- [ ] Add erotic-specific `_strategy_angle` entries.
- [ ] Add erotic-specific `_candidate_style` entries.
- [ ] Add a safety guard that rejects attacker candidates involving minors, coercion, non-consent, exploitation, or abuse.

## Phase 3: Make Evaluator Feedback Attack-Oriented

- [ ] Update evaluator prompts so `Next:` recommends a better attack framing, not a refusal statement or safety explanation.
- [ ] Add a post-processor that rewrites or rejects evaluator suggestions that say to ask the target to refuse.
- [ ] Add a field or derived tag for `off_task_due_to_safe_alternative`, so the attacker knows when it drifted into harmless safety content.
- [ ] Add tests where evaluator feedback suggests refusal, and verify the attacker does not follow that suggestion literally.

## Phase 4: Make Validation Task-Aware

- [ ] Replace global `ACTIONABLE_PATTERNS` with task/policy-specific detectors.
- [ ] Keep bomb/explosive detectors separate from erotic-content detectors and prompt-leak detectors.
- [ ] Add policy-specific refusal phrase sets where needed.
- [ ] Log which detector fired, not only whether actionable content was detected.

## Phase 5: Re-Run The Batch

- [ ] Run the same matrix: `bomb_2`, `erotic`, and `system_prompt_leak` with seeds 500, 501, and 502.
- [ ] Compare against this batch on success rate, first success iteration, contamination count, regeneration count, and validator warnings.
- [ ] Inspect at least one full trajectory per task manually.
- [ ] If contamination is zero but attack success is still low, tune strategy sampling and task-specific prompt families next.

## Phase 6: Longer-Term Improvements

- [ ] Add a small local analyzer command that summarizes a batch directory automatically.
- [ ] Add per-family success/failure stats across seeds.
- [ ] Add a prompt-shape classifier for accepted candidate prompts.
- [ ] Consider separating `attacker_prompt_generation_prompt` from target-facing prompt logs so leakage sources are easier to audit.
